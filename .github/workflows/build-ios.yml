# ============================================
# MyFavor iOS - 在 GitHub Actions 编译生成 IPA
# 用法:
#   1. 手动触发:Actions → "Build iOS IPA" → Run workflow
#   2. 完成后在 Artifacts 下载 MyFavor-unsigned.ipa
#   3. 用 Xcode / Sideloadly / AltStore 装到 iPhone
# ============================================

name: Build iOS IPA

on:
  workflow_dispatch:    # 手动触发(自动触发被禁,iOS build 慢)
  push:
    branches: [main]
    paths:
      - 'MyFavor/**'
      - '.github/workflows/build-ios.yml'

env:
  SCHEME: MyFavor
  WORKSPACE: MyFavor.xcworkspace
  PROJECT: MyFavor.xcodeproj
  CONFIGURATION: Release
  EXPORT_METHOD: development    # 改成 ad-hoc 装到多设备,app-store 上架

jobs:
  build:
    name: Build iOS IPA
    runs-on: macos-14    # macOS 14,带 Xcode 15
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 选择 Xcode 版本
        run: sudo xcode-select -s /Applications/Xcode_15.4.app
      
      - name: 显示 Xcode 版本
        run: xcodebuild -version
      
      - name: 缓存 CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            MyFavor/Pods
            ~/Library/Caches/CocoaPods
            ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-pods-${{ hashFiles('**/*.podspec') }}
          restore-keys: |
            ${{ runner.os }}-pods-
      
      - name: 装 CocoaPods(如需要)
        run: |
          if [ ! -f MyFavor/Podfile ]; then
            echo "❌ 没有 Podfile,跳过(纯 SwiftUI 项目不用 CocoaPods)"
          else
            gem install cocoapods --no-document
            cd MyFavor && pod install
          fi
      
      - name: 列出 schemes
        run: xcodebuild -list -project ${{ env.PROJECT }}

      - name: 编译并生成 IPA
        run: |
          # PROJECT env 是 'MyFavor.xcodeproj'(在仓库根)
          # 所以从仓库根跑,不去 cd 子目录
          mkdir -p build

          # Archive
          xcodebuild \
            -project ${{ env.PROJECT }} \
            -scheme ${{ env.SCHEME }} \
            -configuration ${{ env.CONFIGURATION }} \
            -destination 'generic/platform=iOS' \
            -archivePath build/MyFavor.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            archive
          
          # 检查 archive
          ls -la build/MyFavor.xcarchive/Products/Applications/ 2>/dev/null || echo "Archive 异常"
      
      - name: 创建 ExportOptions.plist
        run: |
          # build 在仓库根(刚才从根跑 archive)
          cat > build/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>development</string>
              <key>destination</key>
              <string>export</string>
          </dict>
          </plist>
          EOF
      
      - name: 手动打包成 IPA(跳过 xcodebuild -exportArchive,它需要 Team)
        run: |
          set -ex  # -e: 出错立即退出  -x: 打印每条命令

          echo "=== PWD ==="
          pwd

          echo "=== build 目录 ==="
          ls -la build/ 2>&1 | head -20 || { echo "❌ build/ 不存在"; exit 1; }

          echo "=== MyFavor.xcarchive/Products/Applications ==="
          ls -la build/MyFavor.xcarchive/Products/Applications/ 2>&1 | head || { echo "❌ xcarchive 找不到"; exit 1; }

          echo "=== .app 内部 ==="
          ls build/MyFavor.xcarchive/Products/Applications/MyFavor.app/ 2>&1 | head -10 || { echo "❌ .app 找不到"; exit 1; }

          echo "=== 创建 Payload ==="
          cd build
          mkdir -p Payload
          cp -r MyFavor.xcarchive/Products/Applications/MyFavor.app Payload/
          ls -la Payload/

          echo "=== 用 zip 打包 ==="
          # 不加 -q,这样能看 zip 输出
          zip -r MyFavor-unsigned.ipa Payload/ -x "*.DS_Store"

          echo "=== 验证 IPA 文件 ==="
          ls -la MyFavor-unsigned.ipa
          echo "--- file 类型 ---"
          file MyFavor-unsigned.ipa
          echo "--- unzip 验证 ---"
          unzip -l MyFavor-unsigned.ipa | head -20

          echo "=== 全部完成 ==="
          pwd
          ls -la *.ipa

      - name: 上传 IPA 为 Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MyFavor-unsigned
          # 明确路径,不用 glob(更可靠)
          path: MyFavor/build/MyFavor-unsigned.ipa
          if-no-files-found: error
          retention-days: 30
